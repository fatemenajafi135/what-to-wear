"""Database-backed repository implementations.

The first package in this project to hold one — feature 004 (closet read) is
also the first feature with a product table to read from. See
`repositories/supabase_closet.py` and `ports.ClosetRepository` for the
Protocol this satisfies.
"""

from __future__ import annotations
